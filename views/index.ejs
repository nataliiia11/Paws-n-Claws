<!DOCTYPE html>
<html lang="en">

    <head>
        <meta name="viewport" content="width=device-width">     
        <link rel="stylesheet" type="text/css" href="../public/css/home.css">       
    </head>

    <body>
        <section id="flash-message-sigin-signup">
            <% if(flashMessages) { %>
                <%if(flashMessages.registered) {%>
                    <%= flashMessages.registered %>
                <%} else if(flashMessages.errorRegister) {%>
                    <%=flashMessages.errorRegister %> 
                <%} else if(flashMessages.notLoggedIn) {%>
                    <%=flashMessages.notLoggedIn %> 
                <%} else if(flashMessages.notAuthed) {%>
                    <%=flashMessages.notAuthed %> 
                <%}%>
           <% }%>
        </section>
        <div id="parallax">
            <section class = "highlights">
                <div class="highlights-background">
                </div>
                <span class="highlights-content">This is a project by Students of HTW in WTAT1</span>
                
            </section>
            <section class = "highlights">
                <div class="highlights-background">
                </div>
                <span class="highlights-content">Members: <br>
                    <ul>
                        <li>Diro</li>
                        <li>Hanh</li>
                        <li>Nhinhi</li>
                        <li>Nata</li>
                    </ul>
                </span>
            </section>
            <section class="highlights">
                <div class="highlights-background">
                    <span class="highlights-content">Paws and Claws is a webapp where Users can share their pet's cute and absurd acts</span>
                </div>
            </section>
        </div>
        <div id="login-container">
            <span id="login-direction">Login to start using</span>
            <div id="login-form-container">
                <span id="close-login-form-container-button">&#10006;</span>
                <form id="login-form">
                    <section id="username-container">
                        <label>Username</label>
                        <Input id="username" type="text" name="username" placeholder="username"></Input>
                    </section>
                    <section id="email-container">
                        <lable>Email</lable>
                        <Input id="email" type="text" name="email" placeholder="email"></Input>
                    </section>
                    <section id="password-container">
                        <lable>Password</lable>
                        <Input id="password" type="password" name="pass" placeholder="password"></Input>
                    </section>

                    <input type="button" id="sign-in-button" value="Sign In"></input>
                    <input type="button" id="sign-up-button" value="Sign Up"></input>
                    <a id="signup-prompt-container">Haven't got an account yet? <span id="signup">sign up</span></a>
                </form>
            </div>
        </div>
    <script src="../public/js/index.js"></script>
    </body>
</html>